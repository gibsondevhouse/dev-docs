# Copy this file to the relevant location and fill in the values.
# Never commit .env files.
#
# For the Python sidecar: copy to dev-docs/src-python/.env
# The app stores the API key in the macOS Keychain at runtime —
# this file is only needed when running the sidecar standalone (outside Tauri).

OPENROUTER_API_KEY=
