# Stage 1 — Connection Test
**Plan:** 002 — Chat and RAG

> **For the agent:** Before starting, confirm the app runs (`npm run dev` opens at `localhost:1420`), the sidecar health check passes, and the Settings screen loads with the existing API key form. When this stage is done, update the changelog.

---

## Stage Goal

The Settings screen has a working "Test Connection" button that calls `POST /chat` via a typed client in `src/lib/api.ts` and displays a green "Connected" badge or a red error message.

---

## Entry Conditions

- [ ] `npm run dev` starts the app without errors
- [ ] `curl http://127.0.0.1:37421/health` returns `{"status":"ok"}`
- [ ] Settings screen renders the API key form

---

## Phases in This Stage

| Phase | Folder | Purpose | Status |
|-------|--------|---------|--------|
| PH1 | `st1-ph1/` | api.ts client + test button in Settings | Not started |

---

## Exit Conditions

- [ ] `src/lib/api.ts` exists with `testConnection` export
- [ ] Settings screen shows "Test Connection" button below the Save button
- [ ] With a valid API key: green "Connected" badge appears after clicking
- [ ] With an invalid key: red error badge appears (no JS exception thrown)
- [ ] Input is disabled and shows "Testing..." while request is in flight
- [ ] No API key logged anywhere
- [ ] Changelog updated
