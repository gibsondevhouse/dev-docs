# Stage 1 — Scaffolding
**Plan:** 001 — Plan Builder UI

> **For the agent:** This document defines Stage 1 of the plan. A stage is a major block of work. Before starting, confirm all prior stages are complete (this is the first stage, so there are none). When this stage is done, update the changelog.

---

## Stage Goal

A working Tauri desktop app shell exists: the Vite + React frontend renders in a native window, the Python FastAPI sidecar starts alongside the app, and the base navigation layout with placeholder screens is in place.

---

## Entry Conditions

- [ ] No prior stages — this is Stage 1. Begin immediately.
- [ ] Node.js (v20+), Rust toolchain, and Python 3.11+ are installed on the machine.

---

## Phases in This Stage

| Phase | Folder | Purpose | Status |
|-------|--------|---------|--------|
| PH1 | `st1-ph1/` | Tauri + Python sidecar setup | Done |
| PH2 | `st1-ph2/` | Base layout + routing | Done |

---

## Stage-Level Setup Tasks

- [x] Confirm `rustup`, `cargo`, `node`, `npm`, and Python 3.11+ are available in PATH before starting PH1
- [x] Create the project root directory `plan-builder/` in a suitable location outside this dev-docs repo

---

## Exit Conditions

This stage is complete when:

- [ ] All phases in this stage are marked done
- [ ] `npm run tauri dev` starts the app without errors
- [ ] A native desktop window opens showing the base layout
- [ ] The Python sidecar starts and `GET http://127.0.0.1:37421/health` returns `{"status": "ok"}`
- [ ] All three placeholder screens are reachable via sidebar navigation
- [ ] Changelog updated with this stage's entry
