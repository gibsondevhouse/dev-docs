# Stage 6 — Plan Preview
**Plan:** 001 — Plan Builder UI

> **For the agent:** Before starting, confirm Stage 5 is complete (planStore is populated from agent JSON output). When this stage is done, update the changelog.

---

## Stage Goal

The Chat screen has a right-side preview panel that renders the plan's folder/file tree. Clicking any file in the tree shows its markdown content in a viewer below the tree, rendered with proper formatting.

---

## Entry Conditions

- [ ] Stage 5 is complete: `planStore` is populated when the agent outputs a plan JSON
- [ ] Chat screen exists with the two-column layout placeholder

---

## Phases in This Stage

| Phase | Folder | Purpose | Status |
|-------|--------|---------|--------|
| PH1 | `st6-ph1/` | Preview panel (file tree + file viewer) | Not started |

---

## Stage-Level Setup Tasks

- [ ] No additional packages needed (react-markdown already installed in ST4)

---

## Exit Conditions

- [ ] All phases in this stage are marked done
- [ ] The Chat screen shows a split layout: chat on the left, preview on the right
- [ ] The preview panel shows the plan file tree (folders and files from `planStore`)
- [ ] Clicking a file node shows its rendered markdown content
- [ ] The preview updates live as the agent updates the plan JSON during the chat
- [ ] Changelog updated with this stage's entry
